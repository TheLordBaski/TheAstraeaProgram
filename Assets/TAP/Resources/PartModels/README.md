# Hand-made part models

Put an FBX named after a part id here (for example `eng_hornet.fbx`) and the game uses it instead of the generated
model for that part. Collider, attach nodes and anchors still come from `Data/parts.json`.

How to export the generated models, edit them in Blender and save them here: `Docs/PART_MODELS.md`.
